#!/bin/bash
# diy-part1.sh — runs BEFORE ./scripts/feeds update -a
# 这里只在需要添加第三方 feed/源码包时才填,目前不需要。
#
# weekdaycare 在这里 clone 了:
#   - luci-theme-aurora      (主题)
#   - luci-app-aurora-config (Aurora 主题配套)
#   - luci-app-bandix        (流量统计)
#   - openwrt-bandix         (bandix 后端)
# 这些我们都不需要,保持空。
exit 0
